{
  "name": "@moolam/template-node-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Sutra node-service integration template — HTTP host with CognitiveCore",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "smoke": "node --experimental-strip-types scripts/smoke.ts",
    "start": "node --experimental-strip-types src/server.ts"
  },
  "dependencies": {
    "sutra-sdk": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "typescript": "^5.8.0"
  }
}
