# voice

A voice-only interaction loop: streaming transcription (with partial segments, as the speech contract requires) feeds a core turn, and the reply streams back through synthesis. Voice-only is a first-class modality for feature phones, driving, accessibility, and low-literacy contexts.

```bash
pnpm voice
```
