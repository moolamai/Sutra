# offline-edge

An `EdgeAgent` completing a full agent turn with zero connectivity: local mock SLM, in-memory storage driver, no sync transport (permanently-offline sovereign mode). Demonstrates that the edge host is complete without a cloud.

```bash
pnpm offline-edge
```
