# prove-companion

Sutra companion project generated by `create-sutra`.

## Binding choices

| Choice | Value |
|--------|-------|
| Domain pack | `custom` |
| Storage driver | `memory` |
| Transport | `offline` |

## Next steps

1. `npm install`
2. `npm run typecheck`
3. `npm run smoke` — runs one mocked CognitiveCore turn
4. Wire production adapters in `src/bindings/` (storage + transport stubs compile as-is)

All dependencies use public `sutra-sdk` semver ranges — no monorepo workspace links.
