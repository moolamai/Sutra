# Stranger-test triage — 2026-07-16

Disposition of [FINDINGS-2026-07-16.md](./FINDINGS-2026-07-16.md) per STRATEST-002.  
Invariant: no blocker waived without a **fix** or an **explicit P7 deferral** ([P7-RFC-ENTRIES.md](./P7-RFC-ENTRIES.md)).

| ID | Severity | Disposition | Evidence |
|----|----------|-------------|----------|
| F-001 | P0 | **Closed — fixed** | `tools/create-sutra/bin/create-sutra.mjs` parenthesizes `.trim() \|\| default`; regression in `scripts/check-stranger-test-protocol.test.mjs` |
| F-002 | P0 | **Closed (doc)** + **P7 defer (production registry)** | Scratch-pack / verdaccio path documented in [`docs/sdk/implementor-quickstart.md`](../../../../../../../sdk/implementor-quickstart.md) §2; production npm publish deferred as [P7-RFC-INTENT-001](./P7-RFC-ENTRIES.md#p7-rfc-intent-001) |
| F-003 | P1 | **Closed — fixed** | Implementor quickstart §2 lists full `@moolam/*` override set including `@moolam/observability` + `pnpm publish:rehearsal:verify` pointer |
| F-004 | P2 | **Deferred — P7 RFC** | [P7-RFC-INTENT-002](./P7-RFC-ENTRIES.md#p7-rfc-intent-002) — publish `@moolam/create-sutra` to scratch registry |
| F-005 | P3 | **Closed — fixed** | Site chrome [`docs-site/src/quickstarts/stranger-test.md`](../../../../../../../docs-site/src/quickstarts/stranger-test.md) carries tester brief + scratch-pack reminder |

## Lessons learned (rolled into quickstarts)

1. Strangers hit **registry 404** before smoke — lead with scratch packs, not `npm install` alone.
2. SDK install pulls **transitive** `@moolam/observability` — overrides must list the full graph.
3. Scaffold CLI must stay parse-safe; contributor path remains `pnpm create-sutra` until P7 publishes the bin.
4. Restart / concurrent turns / `syncAttemptId` already covered — keep them in the implementor failure tables.

## Observability (triage close)

```json
{"event":"docs_site.stranger_test.triage","outcome":"ok","subjectId":"stranger-proxy-2026-07-16","deviceId":"stranger-lab","closedCount":4,"deferredCount":2,"frictionIds":["F-001","F-002","F-003","F-004","F-005"]}
```

Note: F-002 counts as both closed (doc) and deferred (production registry intent) — `deferredCount` includes P7-RFC-INTENT-001 and P7-RFC-INTENT-002.
