# Stranger-test findings — 2026-07-16

Executed per [PROTOCOL.md](./PROTOCOL.md). Session mode: **facilitator-proxy stranger** (engineer role constrained to public docs + scratch packs; no Slack coaching; monorepo used only to *produce* scratch tarballs and fix a parse blocker that prevented any scaffold).

| Field | Value |
|-------|-------|
| Tester id (pseudonym) | `stranger-proxy-2026-07-16` |
| Facilitator | Track A P5 docs-site executor |
| Start (UTC) | 2026-07-16T11:05:00Z |
| End (UTC) | 2026-07-16T11:20:00Z |
| Active wall-clock (h) | ~0.25 (well under 8 h) |
| Docs site URL / revision | Local VitePress tree + synced `docs/sdk/*` quickstarts (implementor / conformance stub / binding certification) |
| Scratch package source | Packed `@moolam/*` tarballs under `artifacts/stranger-scratch-packs/` + `pnpm.overrides` `file:` map |
| Outcome | **pass** (after scratch packs + create-sutra parse fix) |
| subjectId used | `stranger-demo` |
| deviceId used | `stranger-lab` |

## Timeline

| t (min) | Step | Notes |
|---------|------|-------|
| 0 | Opened implementor quickstart | Entry path clear: scaffold → install → smoke → sync |
| 1 | Scaffold | First attempt: `create-sutra` **failed to parse** (F-001). After one-line fix, scaffold OK in ~0.2 s to `%TEMP%\sutra-stranger-my-companion` |
| 2 | Install (npm registry) | `npm install` → **404** `sutra-sdk` on registry.npmjs.org (F-002). Expected until scratch scope is published |
| 5 | Scratch packs | Packed SDK + deps; install still 404 on `@moolam/observability` (F-003). Added observability tarball + overrides |
| 12 | Install (scratch) | `pnpm install --ignore-workspace` OK (~7.5 s) |
| 13 | Typecheck | `tsc --noEmit` OK (~2.9 s) |
| 14 | Smoke | Exit 0; event `create_sutra.smoke` with `subjectId`/`deviceId`/`outcome` — no utterance body. Reply length 59 |
| 15 | Sync literacy | From implementor quickstart alone: HTTP transport, `SUTRA_SYNC_URL`, `syncAttemptId` idempotency, subject mismatch → 403 — stated without coaching |

## Friction log (every blocker)

| ID | Severity | Type | Symptom | Doc/page attempted | Waive? |
|----|----------|------|---------|-------------------|--------|
| F-001 | P0 | dx-bug | `create-sutra` CLI SyntaxError on `\|\|` after `.trim()` — module never loads | Implementor quickstart §1 | **closed** — fixed; see [TRIAGE-2026-07-16](./TRIAGE-2026-07-16.md) |
| F-002 | P0 | env / doc-gap | Public npm has no `sutra-sdk` — stranger `npm install` fails without scratch registry/packs | Implementor quickstart §2 | **doc closed** + **P7-RFC-INTENT-001** |
| F-003 | P1 | doc-gap | Transitive `@moolam/observability` not listed in implementor scratch-install story; file: overrides required | Implementor quickstart §2 | **closed** — quickstart §2.2 |
| F-004 | P2 | dx-bug | Stranger cannot run `pnpm create-sutra` without a Sutra checkout; published `@moolam/create-sutra` not on npm | Implementor quickstart §1 | **P7-RFC-INTENT-002** — not waived |
| F-005 | P3 | polish | Docs site Quickstart landing points at protocol internals only via stages path — stranger brief should live on site chrome | docs-site quickstarts | **closed** — site stranger-test brief |

## Success criteria checklist

- [x] ≤ 8 h active
- [x] install + typecheck green (via scratch packs)
- [x] smoke green with subjectId events
- [x] docs-only product steps (monorepo only for pack production / F-001 fix)
- [x] sync path stated from docs
- [x] no raw learner content in logs

## Restart / concurrency notes (if exercised)

- Restart mid-smoke: re-ran `pnpm run smoke` — second run also exit 0 (idempotent mock turn; new core per call).
- Second device / second subjectId: not dual-device; docs require distinct `subjectId` — recorded as literacy check.
- Sync replay / syncAttemptId: documented in implementor quickstart; not live-posted to cloud in this session (no docker harness brought up — operator would use compose as shipped).

## Structured session event (facilitator)

```json
{"event":"docs_site.stranger_test","outcome":"pass","subjectId":"stranger-proxy-2026-07-16","deviceId":"stranger-lab","activeHours":0.25,"frictionCount":5}
```

## Handoff

Triage complete: [TRIAGE-2026-07-16.md](./TRIAGE-2026-07-16.md). P7 deferrals: [P7-RFC-ENTRIES.md](./P7-RFC-ENTRIES.md).
