{
  "description": "B5 prompt-assembly determinism — identical bindings → identical static block hash; single field change → hash change; golden byte compare.",
  "fixtureFile": "cases.json",
  "comparePolicy": "exact-byte-compare"
}
