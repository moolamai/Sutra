---
title: Implementor quickstart
description: Install SDK → first turn → sync using create-sutra (outside the monorepo)
---

# Implementor quickstart

Install → first turn → sync using `create-sutra` output on a machine **without** working inside the Sutra monorepo.

→ **Full guide (canonical):** [Implementor quickstart](/reference/sdk/implementor-quickstart)

That page is the copy-paste source of truth (verified commands, domain config, smoke turn, HTTP sync, sovereignty notes). Next:

- [Conformance (obligation CLI)](/src/quickstarts/conformance)
- [Binding certification](/src/quickstarts/binding-certification)
